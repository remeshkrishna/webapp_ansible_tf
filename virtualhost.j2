server {
        listen 8080 default_server;

        root /tmp/project_webapp;

        index index.html;

        server_name {{  ansible_default_ipv4.address }};

        location / {
                try_files $uri $uri/ = 404;
        }
}
